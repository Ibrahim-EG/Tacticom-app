// Top-level build file. Plugin versions are declared here and applied
// (without a version) in app/build.gradle.kts -- this is the standard
// modern Gradle "plugins block" pattern, avoids the old buildscript{}
// classpath juggling that trips people up most often.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
